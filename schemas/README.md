# Schemas

Schemas live here only when an implemented UCHC surface requires a stable
machine-readable contract.

Do not invent a universal gonol tuple or cross-language schema merely to fill the
directory. UCNS owns its geometry contracts; domain implementations own their
language-specific construction records.

hmmm: first UCHC-owned schema will be selected during implementation migration.
