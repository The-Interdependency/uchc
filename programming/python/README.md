# Python UCHC

Status: **active source exists; migration pending**.

Current implementation source:

`The-Interdependency/stack@ca190204de25de240662bb438af80c8dc405cea6/research/python-gonol/`

Current language profile: Python 3.12 source, with the existing Stack baseline pinning
CPython v3.12.14 as its recognition witness.

## Current implemented structure

Exact decoded source occurrences construct through shared glyph identities while
preserving source order, multiplicity, addresses, lines, columns, and provenance.

TAB, LF, FF, and CR have explicit constructions. LF, CR, and CR+LF remain
source-distinct logical-newline constructions. Tokens and AST nodes may verify the
construct; they do not substitute for it.

UCNS owns Public Gonol geometry and positions.

## Migration guidance

Move the existing implementation and tests before extending the Python hyperspace
model. Preserve its existing carrier pin and fail-closed handling of unresolved
off-carrier characters.

hmmm: deeper geometric functions and arbitrary-Unicode admission remain unresolved.
